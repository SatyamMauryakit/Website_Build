/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['lh3.googleusercontent.com', 'res.cloudinary.com', 'cdn.pixabay.com', 'images.unsplash.com', 'avatars.githubusercontent.com']
    }
};

export default nextConfig;
